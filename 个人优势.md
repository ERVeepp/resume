# 个人优势

> 生成日期：2026-08-19 ｜ 模型：GitHub Copilot（DeepSeek V4 Pro）
> 依据：`github.com/ERVeepp/docs/` 个人知识库（代码规范、踩坑记忆、业务文档、AI 资产体系）+ 13 个个人项目代码仓库分析
> 更新：2026-08-19 补充广告归因反欺诈纵深能力（AppsFlyer 防御体系研究 + 红蓝对抗系统）
> 更新：2026-08-20 补充 Codebase RAG 落地（tree-sitter 代码索引，黄金集命中率 80%）+ Pixelle-Video 多模态生成实践
> 更新：2026-08-24 补充广告投放 Agent 全链路落地（交互/执行/风控/策略/创意/验证 5 服务）
> GitHub：[github.com/ERVeepp](https://github.com/ERVeepp)

---

## 一、全栈能力：从前端到底层无断层

- 前端：Vue 3 组件架构、低代码编辑器、大屏数据看板、Electron 桌面应用、开源组件库（npm 发布 dragsection，获 9 个下游项目依赖）
- 后端：NestJS/Express/Spring Boot 服务开发、Redis/ClickHouse 数据架构、Flink 实时计算
- 底层：Rust → WASM 高性能计算、Go 后端服务开发（OpenRTB Bidder、点击引擎）
- 测试：Vitest 单元测试 + Playwright CDP 真机测试 + Rust Fuzzer

**一句话**：不满足于"前端调接口"，能做编辑器引擎核心抽象、协议引擎架构设计、反作弊体系从零搭建。

---

## 二、广告技术深耕：从投放到归因反欺诈的全链路理解

- 覆盖 DSP（投放管理、活动配置、报表分析）→ SSP/ADX（媒体变现、竞价撮合）→ 聚合平台的完整链路
- 自研 IAB MRAID 3.0 / VAST 4.0 协议引擎，不是"调 SDK"而是"造 SDK"
- 反作弊攻防双视角：防守侧设计 6 层防护体系，攻击侧用 Playwright CDP 真机验证
- 实时竞价数据链路：Go Bidder（OpenRTB 2.5）→ Kafka → Flink 5s 窗口聚合（事件时间 + Watermark）→ ClickHouse，落地 RPS/胜率/eCPM 类实时指标
- **投放 Agent 全链路落地**：从"运营一句话起投"到"效果增量验证"的完整闭环——交互层（launch 自然语言解析意图创建 campaign）→ 执行层（媒体 Adapter：多租户 RBAC + 花钱动作门禁 + 审计带 actor）→ 风控层（预算熔断 + 异常消耗告警）→ 策略层（营销策略引擎：流量调控 + 权益分层 + 黄金集评估）→ 创意层（DCO 动态组材）→ 验证层（AB 实验 lift 增量判断），5 个 Go 服务 + 实时数据链路，一台机器可现场演示；离线管道按整合者判断合理暂缓（无算法模型即空转）

### 归因反欺诈纵深

- **归因平台防御体系研究**：系统拆解 AppsFlyer 四层防御（Cloudflare 网络层 → 服务端校验 → SDK 客户端层 → Protect360 归因反欺诈层）+ SKAdNetwork 苹果侧验证，覆盖 15 个检测维度（CTIT、点击泛滥、IP-设备比、设备农场、事件序列分析 Series-of-Events、App-to-App 欺诈等）
- **红蓝对抗系统自建**：独立搭建蓝方（Express 模拟 AppsFlyer + Google Ads 双归因平台，含 Protect360 6 条规则 + Google 7 条反欺诈规则 + last-click 归因引擎）+ 红方（5 种已实现攻击方案：curl flooding / Playwright+设备指纹库 / CDP 真机 / CloakBrowser，11 种攻击方案 spec 规划）
- **反侦测技术实践**：浏览器指纹伪装（WebGL/canvas/字体注入）、CDP 反检测（webdriver/__playwright__ 标记去除）、Cloudflare 绕过（cf_clearance cookie 池 + TLS 指纹伪装 curl-impersonate）、设备指纹库建设
- **攻防方法论沉淀**：对外防御文档（给客户）+ 内部解决方案文档（核心知识）双文档体系，攻击手法与防御弱点成体系对照

---

## 三、AI 工程化实践：LLM 应用 + Agent 平台 + RAG 全栈深度

- 大模型应用：从零搭建多 Agent 平台（matrix-ai-agent），SSE 流式输出、Function Calling 工具编排、意图路由（关键词 + LLM 分类）、OpenAI 兼容协议接入，已落地广告平台，运营用自然语言做数据查询
- **AI Coding 工具深度使用**：日常用 Kiro / VS Code Copilot / DeepSeek Harness（DSH）做 Vibe Coding（Spec 驱动 + 上下文工程），理解 Agentic Loop / Tool Calling / Sub-agent 委派 / Prompt Engineering 底层机制
- RAG 知识增强：KnowledgeAgent 从关键词搜索升级为向量库语义检索——分块 → Embedding → Qdrant 存储 → 向量召回 + 引用溯源，多租户 productId payload 过滤；重排/混合检索列入迭代计划
- **Codebase RAG 落地**：tree-sitter（WebAssembly，免编译）按函数/类边界解析代码库，提取 符号/签名/实现体 → Embedding → Qdrant `code_docs` → 代码语义检索 + 黄金集评估（top5 命中率 80%），支撑 AI 生成/修改代码前检索现有实现与调用关系
- **多模态素材检索（CLIP）**：CLIP 图文同空间（ViT-B/32 512 维）对素材库图像编码入 Qdrant，文搜图/图搜图 + 黄金集评估，图搜图自检命中率 100%（Python + mise 环境）
- **素材合规巡检（PaddleOCR）**：文生图素材的字画在像素里、不在文本域，用 PaddleOCR 提取图上文字过广告法违禁词库 + OCR 置信度筛乱码/鬼画符 + 检测框判截断遮挡，与 CLIP 构成「CLIP 看语义 + OCR 看字」双层合规门禁
- 编排框架实践：LangChain（RAG 链：文档加载/分块/Embedding/向量库组件）+ LangGraph（多 Agent 图编排）实战，坚持「RAG 用框架库、Agent 循环与路由自研」的选型原则
- 协议与标准：MCP 双端实践（client 接外部工具 + server 暴露自家工具）、SKILL.md、Function Calling、SSE 流式
- 多模型与可观测：chat/embedding 模型分离支持热切换，token 用量 + 费用实时追踪，量化模型分级省钱策略
- AI 评测体系：评测四层**全部落地**——前三层（语法 checkSyntax / 契约 runContractTest / 黄金集 regressionTest）前置成门禁、复用沙箱把"事后回归"升级为"事中门禁"；第四层 LLM-as-Judge（judgeAnswer 四维打分）落地为事后语义评审
- Agent 宿主集成：VS Code 插件（Agnes AI - Copilot Chat Provider）+ Electron 桌面壳（deepseek-harness-desktop）+ **DSH 插件生态开发（dsh-plugins，2 个插件已上线）** 三形态——Electron 内置 Node 运行时（`ELECTRON_RUN_AS_NODE=1`，终端用户免装 Node）、进程托管（spawn / 就绪探测 / 健康检查 / 异常自动重启 / 优雅回收）、版本管理（内置 pnpm 按版本动态安装 npm 包，升级只换版本号不改壳）；DSH 侧理解 Cordis 插件模型与 bundle 规范，npm 包引入审查 + 硬件性能评测两插件已收录 1024Store 市场并发布 npm
- 不只是用 Copilot 聊天补全，而是搭了一套完整的三层 AI 资产生命周期体系（instructions/memories/skills/agents）
- 开发了编译期标签矩阵 + 一源多发同步脚本，准确将规范注入到 6 个编辑器
- Spec-Kit Lite 需求驱动流程：歧义扫描 → 强制澄清 → 出口校验，10 个活跃 spec 落地执行
- VS Code 插件已上架 Marketplace
- 多模态生成实践：基于 Pixelle-Video（开源全自动短视频引擎）跑通「主题 → 完整视频」端到端流水线（LLM 文案 + 文生图/视频 + TTS + 合成），理解 AI 创作平台架构

---

## 四、工程素养：全链路交付与前端部署（FDE）

- Monorepo 架构设计（pnpm workspace + Changesets 版本管理）、体积门禁 CLI、多环境构建配置
- 前端部署：Nginx 反向代理（SPA try_files、gzip、缓存分层）、Docker 容器化（多阶段/产物镜像、docker-compose）、CI/CD 流水线
- 流式部署：SSE 长连接透传（proxy_buffering off）、推理超时调优，保证 AI 应用打字机体验
- AI 代码回归测试：落地四层方案（语法校验 → 契约测试 → 黄金测试集 → LLM-as-Judge），把"对比历史"变成"对比契约"，解决 AI 生成代码无基线可比的难题
- 从项目搭建 → 规范制定 → Spec 驱动 → CI/CD → 部署的完整交付能力
- Git 工作流规范、Husky + commitlint 代码质量管控

---

## 五、自驱学习：五语言 + 多领域

| 语言/技术 | 掌握程度 | 证据 |
|-----------|:--:|------|
| TypeScript/JavaScript | ⭐⭐⭐⭐⭐ | 主力语言，所有前端 + Node 后端项目 |
| Rust | ⭐⭐⭐ | WASM 编译、CLI 工具、Fuzzer |
| Node.js 后端 | ⭐⭐⭐⭐ | NestJS 服务架构、API 设计、数据库交互 |
| Go | ⭐⭐⭐ | OpenRTB 2.5 Bidder（ad-tech-labs）、点击引擎（buzz_server）、Kafka 异步 |
| Java | ⭐⭐⭐ | Spring Boot 3 + SseEmitter 流式（ai-chat-demo，对应 NestJS 流式实现的 Java 版） |
| Flink | ⭐⭐ | 实时窗口聚合（SQL/PyFlink，ad-tech-labs 竞价指标链路） |
| Python | ⭐⭐⭐ | PaddleOCR（素材合规巡检：文字提取/违禁词/乱码置信度）+ airtest 移动端 UI 自动化（OpenCV）+ Chrome cookie 解密（AES/SQLite）+ 云手机自动化脚本 |
| Docker | ⭐⭐⭐ | 多项目容器化部署 |

---

## 六、核心差异化

> "一个能从编辑器引擎抽象做到 Rust/WASM 协议解析，从广告归因反欺诈攻防做到 AI Agent 平台 + RAG 知识增强，再到流式前端部署全链路交付的人。"

---

## 七、简历版（可直接放入简历"个人优势"栏）

> 精简版，3-5 条，每条一句话，突出差异化。按目标岗位可裁剪。

**综合版（最终定稿）**：

一句话定位：**全栈纵深 + 广告技术 + AI Agent 三线并进，能独立完成从底层协议到应用产品的全链路交付。**

1. **全栈纵深**：Vue 3 前端 + NestJS/Go 后端 + Rust/WASM 底层三层贯通，独立完成 40+ 业务页面、协议引擎、后端服务全栈开发
2. **广告技术全链路**：DSP/SSP/ADX 全链路 + 自研 IAB MRAID 3.0/VAST 4.0 协议引擎 + 归因反欺诈（AppsFlyer 四层防御研究 + 红蓝对抗系统 + 6 层反作弊体系）+ 素材合规巡检（PaddleOCR 提取素材内文字过违禁词 + CLIP 语义双层门禁）
3. **AI Agent 全栈**：从零搭建多 Agent 平台（SSE 流式 + Function Calling + 意图路由 + 文档/Codebase/多模态三 RAG）+ agent-core 核心库 + VS Code 插件（已上架）+ Electron 桌面壳 + **DSH 插件生态（2 个已上线 1024Store + npm）** + 多模态生成流水线（Pixelle-Video）+ 日常用 Kiro / VS Code Copilot / DeepSeek Harness（DSH）做 Vibe Coding（理解 Sub-agent 委派 / Prompt Engineering），覆盖平台/架构/双端宿主/生态插件
4. **AI 工程化**：三层 AI 资产生命周期 + 编译期标签矩阵一源多发 6 编辑器 + Spec 驱动开发 + 评测四层全落地（前三层前置门禁 + LLM-as-Judge 抽检），让 AI 协作可复制、可度量
5. **工程素养**：Monorepo + 体积门禁 CLI + CI/CD + Docker 部署 + 流式部署调优的完整交付能力
6. **开源贡献**：npm 开源组件 dragsection（获 9 个下游项目依赖）+ GitHub 开源项目（github.com/ERVeepp）

**AI Agent + 全栈综合版（推荐）**：

1. 全栈工程能力：Vue 3 前端 + NestJS/Go 后端 + Rust/WASM 底层，独立完成 40+ 页面全栈开发与协议引擎架构设计
2. AI Agent 平台：从零搭建多 Agent 协作平台，SSE 流式输出 + Function Calling 工具编排 + 意图路由（关键词 + LLM 分类）+ OpenAI 兼容协议
3. Agent 架构与产品化：agent-core 核心库独立构建发布，VS Code 插件已上架 Marketplace，Electron 桌面壳（内置 Node 运行时 + 进程托管 + 版本管理）
4. AI 资产体系与评测：三层 AI 资产生命周期（instructions/memories/skills/agents）+ 编译期标签矩阵 + 一源多发 6 编辑器 + 评测四层全落地（语法/契约/黄金集前置门禁 + LLM-as-Judge）
5. 广告技术纵深：DSP/SSP/ADX 全链路 + 自研 IAB MRAID 3.0/VAST 4.0 协议引擎 + 归因反欺诈红蓝对抗（AppsFlyer 四层防御 + 6 层反作弊）+ 素材合规巡检（PaddleOCR 字层 + CLIP 语义双层门禁）
6. 工程素养：Monorepo + 体积门禁 CLI + Spec 驱动开发 + CI/CD + Docker 部署的完整交付链路

**全栈版**：

1. 全栈工程能力：Vue 3 前端 + NestJS/Go 后端 + Rust/WASM 底层，独立完成 40+ 页面全栈开发与协议引擎架构设计
2. 广告技术纵深：覆盖 DSP/SSP/ADX 全链路，自研 IAB MRAID 3.0 / VAST 4.0 协议引擎，深耕归因反欺诈（AppsFlyer 四层防御体系 + 红蓝对抗验证），素材合规巡检（PaddleOCR 违禁词 + 乱码置信度筛选）
3. AI 工程化：从零搭建多 Agent 平台（SSE 流式 + Function Calling + 意图路由），构建三层 AI 资产生命周期体系，VS Code 插件已上架 Marketplace
4. 攻防一体：防守侧设计 6 层反作弊体系，攻击侧用 CDP 真机 + 反侦测技术（浏览器指纹伪装、Cloudflare 绕过）验证并迭代加固
5. 工程素养：Monorepo + 体积门禁 CLI + Spec 驱动开发 + CI/CD + Docker 部署的完整交付链路

**前端岗版（裁掉后端/AI 深度）**：

1. 全栈前端：Vue 3 + TypeScript + Pinia + Element Plus + ECharts，独立完成 DSP/SSP 平台 40+ 页面
2. 性能优化：Rust/WASM 数据处理性能提升 15 倍（30s → 2s）
3. 工程化：Monorepo（pnpm）+ 体积门禁 CLI + 多环境构建 + Nginx 部署
4. 反作弊：客户端 Bot 检测 + 行为追踪 + 请求签名 + 限流的 6 层防护体系

**AI Agent 岗版（裁掉广告业务细节）**：

1. AI 平台开发：从零搭建多 Agent 协作平台（NestJS + OpenAI 兼容协议），SSE 流式输出 + Function Calling 工具编排 + 意图路由
2. RAG 知识增强：向量库语义检索（Qdrant + Embedding + 重排 + 引用溯源 + 多租户隔离），LangChain/LangGraph 实战 + 自研选型
3. Agent 架构：agent-core 核心库独立构建发布，与业务解耦，可复用于下游项目
4. AI 资产体系与评测：编译期标签矩阵 + 一源多发，23 份规范 + 17 个技能 + 3 个 Agent 精准注入 6 个编辑器；评测四层全落地（Harness 守门员）
5. 产品化：VS Code 插件已上架 Marketplace，Electron 桌面壳（内置 Node 运行时 + 进程托管 + 版本管理），Docker 容器化部署

