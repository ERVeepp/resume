# BOSS 直聘「字数」统计口径备忘

> 背景：写「项目业绩/项目描述」时本地数出来 ~993 字（不含空格），贴到 BOSS 直聘却显示 830，反复压缩仍对不上——因为 **平台的字数口径 ≠ 本地 `len()` / Word 字数**。
> 本文记录反推出的平台口径 + 本地换算方法，避免再瞎压。

---

## 一、BOSS 直聘到底怎么数

实测反推，BOSS 直聘（网页版填写「项目经历」文本域）的字数统计口径接近：

$$\text{平台字数} = \text{汉字数} + \text{英文字母数} + \text{数字位数}$$

即：**每个中文字算 1 字；每个英文字母、每个数字各算 1 字**（`SQLite` = 6 字、`0.2ms` = 4 字）。**空格、换行、标点符号、`+`/`/`/`→` 等符号不计或基本不计。**

### 反推验证（以 matrix-ai-agent「项目业绩」两版实测为例）

| 口径 | 压缩版 | 重铺版 |
|------|:---:|:---:|
| 本地 `len()` 含空格 | 1148 | 1363 |
| 本地去空格 | 993 | 1179 |
| 纯汉字 | 382 | 483 |
| **汉字 + 英文字母 + 数字（本地估算≈平台）** | **847** | **1010** |
| **BOSS 直聘实际显示** | **830** | **973** |
| 本地估算 − 实显 | +17 | +37 |

**校准结论**：BOSS 实际显示 ≈ 本地「汉字+字母数字」口径的 **~0.96 倍**（两次均少 2%~4%）。

本地换算系数建议：$$\text{BOSS 字数} \approx (\text{汉字}+\text{字母}+\text{数字}) \times 0.96$$

> 想平台显示 ~1000 → 本地「汉字+字母数字」写到 **~1040**（约多 4%）。
> 差异来源猜测：半角符号/部分连字符（`_` `-` `.`）或个别英文的处理口径不同，不必死抠，用系数粗调即可。

---

## 二、本地怎么快速换算

> 目标 ~1000 平台字 = 汉字 + 英文数字字符 ≈ 1000。

用 Python 一行统计（PowerShell 里跑）：

```powershell
python -c "import re;t=open('resume/项目经历-AI原版.md',encoding='utf-8').read();s=t[t.index('**项目业绩**'):t.index('---',t.index('**项目业绩**'))];b=''.join(re.findall(r'^- (.+)$',s,re.M));print(len(re.findall(r'[\u4e00-\u9fff]',b))+len(re.findall(r'[A-Za-z0-9]',b)))"
```

口径里应扣除/注意：
- **不算**：空格、换行、`/`、`+`、`→`、`·`、全角括号等标点
- **算**：每个汉字、每个英文字母、每个数字（含版本号里的 `.`/`-` 是否计入存疑，测试时发现差异约 ±20，不必死抠）

---

## 三、平台口径下的写作策略（重要）

英文和数字在平台口径里**很贵**——`LangGraph` 一个词就占 9 字，`verify-resume` 占 14 字。所以：

1. **优先中文叙事的量化**：中文 1 字表达的信息量 >> 英文。能用中文说清的（"断点续跑"4 字 vs `checkpoint resume` 17 字）用中文。
2. **技术名词保留英文但只出现一次**：`LangGraph`、`Qdrant`、`Function Calling` 首提即可，后续用中文代称（"引擎/向量库/工具循环"）。
3. **数字尽量精炼**：`0.2ms vs 16.6ms` 占 12 字，若只传递"快 80 倍"就写"0.2ms/16.6ms"或直接"快约 80 倍"（7 字）。
4. **版本号、长英文标识符能省则省**：`TOKEN_DAILY_LIMIT`（18 字）可简写 `TOKEN_DAILY_LIMIT` 保留但少用，或说"日配额 env"。

### 反直觉点
- 本地去空格后 993 字看着接近 1000，平台口径却只有 ~830 → 因为英文按词被本地当成 1 个"词"，平台按字母逐个算，两边一进一出，**本地数字不可信，以平台口径换算为准**。
- 想平台显示 ~1000，本地（含空格）通常要写到 **1150~1200** 才够。

---

## 四、各文本域建议上限（经验值）

| BOSS 字段 | 平台字数建议 | 备注 |
|-----------|------------|------|
| 项目经历-项目描述 | ~1000 | 业绩亮点集中在此 |
| 项目经历-项目业绩 | ~800-1000 | 多用中文量化、少堆英文 |
| 个人优势/自我介绍 | ~500 | 首屏抓重点 |

> 平台本身有硬上限（通常 2000+），超了会截断；这里的建议是「可读性上限」，不是平台限制。

---

## 五、本次实操（2026-09-07 matrix-ai-agent 业绩）

- 版本演进：压缩到 993（本地去空格）→ 平台显示仅 830 → 反推平台口径 → 按平台口径重铺，补回被误删的硬信息（`PaddleOCR 合规门禁`、`verify-resume 端到端验证`、`reason` 三段式）。
- 目标：平台口径 ~1000；当前落点见业绩区（`项目经历-AI原版.md`）。
