# 个人优势

**📌 简历一句话总结（放简历最顶端）**：
> 十年广告科技前端 + AI Agent 工程化双栈（Vue3/React + Node/NestJS + Rust/WASM 三层贯通），独立交付 40+ 页面、支撑日均百万级广告请求；从零自研 IAB 广告协议引擎（Rust/WASM 解析 7.6µs）与 AI 工程化闭环（Spec 驱动 + 契约/黄金集门禁 + 批量生成，60+ spec），具备从需求到上线端到端交付能力。

> 用途：直接上简历。第一人称叙述，讲清楚"擅长什么、怎么来的、做出过什么"。
> 生成：2026-08-25 ｜ **模型：GitHub Copilot（DeepSeek V4 Pro）**
> 依据：`github.com/ERVeepp/docs/` 个人知识库 + 项目代码仓库分析 + 个人简历材料；**内容数据均按项目实际核实（属实）**
> 拟人化版本；AI 生成的旧版保留在「个人优势-AI原版.md」

---

## 一、全栈，不设边界

前端 Vue 3 是主力，后端 NestJS/Go 也写过，底层还能用 Rust 编 WASM。不是"会一点"，是每条线都有真东西：40+ 业务页面是前端，广告协议引擎是底层，落地页平台的后端也是我写的。Rust/WASM 那件事最能说明问题——CSV/Excel 解析原来纯 JS 跑 30 秒，用户等得着急，我用 Rust 重写后 2 秒出结果，快了一个数量级。

## 二、广告行业，从投放到归因反欺诈

在程序化广告做了十年，从 DSP 投放管理到 SSP 媒体侧、ADX 对接，全链路都摸过。不喜欢"调现成 SDK"，而是自己造了一个 IAB 协议引擎（MRAID 3.0 / VAST 4.0）。反作弊这块走得比较深：防守侧设计过 6 层防护，攻击侧用 Playwright CDP 直连真机去攻自己——红蓝对抗，把 AppsFlyer 四层防御拆了个遍，找出过 WebDriver 绕过、请求时序识别不足这些盲区，再回头加固防守。素材合规也用 PaddleOCR + CLIP 做了"看字 + 看语义"的双层门禁。

## 三、AI 工程化，从"用工具"到"造流水线"

早期用 Copilot 就是聊天补全，后来发现 AI 写代码质量不稳，就开始搭自己的体系：
- **AI 资产**：把规范、踩坑记忆、技能、Agent 组织成三层资产，写了个编译期脚本按项目技术栈标签精准注入到 6 个编辑器——AI 动手前先拿到对的规范，注入从 8 份收敛到 4 份。后又做了**注入体量治理**：诊断出注入文件 22KB 里 68% 是索引表，改成“名称 + 一句话 + README 指针”后降到 15.6KB（-29%），上下文工程从定性到可量化。
- **Spec 驱动**：openspec 基座 + 门禁插件（歧义扫描、强制澄清、契约验收、黄金集回归），60+ 活跃 spec，需求到归档有机器把关，不靠记性；openspec CLI 已接到真实业务项目（v1 需求基线化 + `openspec validate` 结构门禁），不是只搭体系，是标准工具在真实业务跑通。
- **多 Agent 平台**：SSE 流式 + Function Calling + 三形态 RAG（文档 / 代码 / 多模态），agent-core 核心库独立打包。
- **长任务执行平台**：在底座上升级长任务可靠执行（断点续跑 / 重试 / 心跳 / 预算熔断）+ 广告全链路 Agent 化（**投放 Agent 已落地**：一句话起投 / 调价 / 防超预算，5 Go 服务 + spec 归档；报表 / 巡检 / 竞品持续推进），花钱动作过写三档 + 门禁 + 审计，决策可解释可回测。
- **批量生成流水线**：模板 + 参数批量出变体，过了语法/契约门禁才自动建分支提 PR，还带 CI 防退化。
- **外延**：VS Code 插件已上架，DSH 桌面壳（内置 Node 运行时 + 进程托管），还写了两个 DSH 插件上了 1024Store 和 npm。

## 四、交付能力

Monorepo（pnpm + Changesets）、体积门禁 CLI、CI/CD、Docker 部署、流式部署调优（SSE 透传、推理超时），从搭项目到上线能自己闭环。git 工作流、Husky + commitlint 代码管控也管得起来。

## 五、学习能力

四门语言 + Flink 都真用过，不是"了解"：TS/JS 是主力；Rust 编译 WASM；Go 写过 OpenRTB Bidder 和点击引擎；Python 用来做 OCR 和 UI 自动化；Flink 做实时窗口聚合。

## 六、一句话

> "能从编辑器引擎抽象做到 Rust/WASM 协议解析，从广告归因反欺诈攻防做到 AI Agent 平台 + RAG 知识增强，再到流式前端部署全链路交付的人。"

---

## 七、简历版（个人优势 · 整块复制即用）

> 📋 **以下一整块可直接复制到简历 / Boss直聘 在线简历"个人优势"栏，复制到「--- 结束」为止。**

---

**个人优势**：

0. **十年程序化广告前端**
1. **全栈纵深**：Vue 3 前端 + NestJS/Go 后端 + Rust/WASM 底层三层贯通，独立完成 40+ 业务页面、协议引擎、后端服务全栈开发
2. **广告技术全链路**：DSP/SSP/ADX 全链路 + 自研 IAB MRAID 3.0/VAST 4.0 协议引擎 + 归因反欺诈（AppsFlyer 四层防御 + 红蓝对抗系统）+ 素材合规巡检（PaddleOCR + CLIP 双层门禁）
3. **AI Agent 全栈**：从零搭建多 Agent 平台（NestJS + SSE 流式 + Function Calling + 意图路由 + 三 RAG：文档/Codebase/多模态，Qdrant 向量检索 + 引用溯源 + 多租户隔离，命中率 80%/100% 由自建黄金集评测验证，LangChain/LangGraph 实战）+ **长任务执行平台**（断点续跑/心跳/预算熔断，广告全链路 Agent 化：**投放 Agent 已落地**，花钱动作过写三档 + 门禁 + 审计，决策 JSON 可回测）+ agent-core 核心库独立构建发布（与业务解耦、可复用）+ VS Code 插件（已上架）+ DSH 插件生态（2 个上线）+ 多模态生成流水线（Pixelle-Video）
4. **AI 工程化**：三层 AI 资产生命周期 + 编译期标签矩阵一源多发 6 编辑器 + Spec 驱动开发（openspec 基座 + 门禁插件，CLI 已落地真实业务项目）+ 上下文注入治理（22KB→15.6KB，-29%）+ 资产文件化与本地 RAG（黄金集命中 100%）+ 评测四层全落地（Harness 守门员：语法 / 契约 / 黄金集 三层前置门禁 + LLM-as-Judge 抽检，黄金集回归脚本可复现）+ 产品化（Electron 桌面壳 + Docker 容器化部署）
5. **工程素养**：Monorepo + 体积门禁 CLI + CI/CD + Docker + 流式部署调优的完整交付能力
6. **开源贡献**：npm 组件 dragsection（9 个下游依赖）+ GitHub 开源项目（github.com/ERVeepp）

**技能标签**（可直接粘贴到在线简历"技能标签"栏）：

> TypeScript / Python / Go / Rust ｜ AI Coding / SDD / Harness / Agentic Loop / Function Calling / Multi-Agent / RAG / MCP / SSE 流式 / Temporal 长任务编排 ｜ LangChain / LangGraph / OpenAI 兼容 ｜ Vue 3 / React / Pinia / Element Plus / ECharts / Vite ｜ NestJS / Node.js ｜ Redis / MySQL / ClickHouse / Kafka / PyFlink / Qdrant ｜ 程序化广告：DSP / ADX / SSP / DMP / OpenRTB 2.5 / 竞价策略 / 投放 Agent / 反作弊 ｜ Vitest / Playwright / 黄金集评测 / LLM-as-Judge ｜ CI/CD / Docker / Nginx / Linux

---

> ✅ **复制结束**

---

### 备注（勿粘贴，供参考）

**① Boss直聘 / 在线简历填写提示**：

| 环节 | 动作 |
|---|---|
| **期望职位** | 填「前端Agent开发」或「AI 研发工程师」，与目标 JD 对齐 |
| **技能标签** | 手动补：AI Coding、SDD、Harness、RAG、Codebase RAG、Function Calling、LangGraph、CI/CD |
| **项目描述** | 粘贴上面个人优势 + 项目经历（保留关键词 + 量化数字） |
| **打招呼话术** | 带 pipeline 关键词 + 匹配点 + 量化（完整模板见下） |
| **活跃度** | 保持在线 + 定时刷新（活跃度影响曝光排序）；投递后跟进沟通 |

**pipeline 描述（简历正文用，可放"个人优势·AI 工程化"或项目经历）**：

> 自建端到端 AI 工程化流水线：需求经 **Spec 驱动**锁死（openspec 基座 + 歧义扫描 / 强制澄清 / 出口校验三道门禁，60+ 活跃 spec 全程机器把关）→ **契约化**（需求转 `input→expected` 机器断言）→ **批量生成**（模板 + 参数化批量出变体，过语法 / 契约门禁自动建分支提 PR）→ **评测四层**（语法 / 契约 / 黄金集确定性前置 + LLM-as-Judge 主观抽检）→ **CI 回归**（黄金集退化率 >0 拦发布），以首次通过率 / 人工干预率 / 交付时长量化 AI 生成质量，实现从需求到发布的全自动验收闭环。

**打招呼话术（完整模板，按投递方向选）**：

> **① AI Coding / Agent 方向（主推）**：
> "您好，十年广告科技前端（Vue3/React + Node/NestJS + Rust/WASM），自建了端到端 AI Coding Pipeline：**Spec 驱动**（openspec 基座 + 60+ spec 机器把关）→ **Harness 评测门禁**（语法 / 契约 / 黄金集三层确定性前置，主观才用 LLM 抽检）→ **批量生成**（模板 + 参数出变体，过门禁自动分支 / PR）→ **CI 回归**（退化率 >0 拦发布），首次通过率量化质量。看到贵司做 AI 研发方向，与我的工程化能力正好匹配，期待沟通。"
>
> **② 短版（通用投递）**：
> "您好，十年前端 + AI Agent 双栈，独立交付 40+ 页面，自建 AI Coding 流水线（Spec → Harness → 批量 → CI，60+ spec 机器把关）做研发流程 AI 化。对贵司岗位很感兴趣，方便聊聊吗？"
>
> **③ 前端方向**：
> "您好，十年广告科技前端，Vue3/React + Node + Rust/WASM 三层贯通，同时自建 AI 工程化体系（Spec 驱动 + 评测门禁，一源多发 6 编辑器）。想了解贵司前端与 AI 的结合点，期待沟通。"

> **④ 快手广告 AI 应用方向（实际投递格式，直接发）**：
> "BOSS您好，我想应聘贵司的 AI 应用研发工程师（快手广告方向）职位。我有 10 年广告平台研发经验，擅长 Vue3 / React、Node 全栈、容器化部署、AI Agent 应用、AI Coding 流水线等，期待您的回复。"

**自定义技能池（按岗位挑 4-6 个，每个 ≤8 字）**：
> 广告平台研发 / Vue3 / React / Node 全栈 / 容器化部署 / AI Agent 应用 / AI Coding 流水线 / Rust·WASM / RAG 检索 / 端到端交付

**② 评估与评分说明**（数据来源可信度）：

- **生成**：GitHub Copilot（DeepSeek V4 Pro），基于 `github.com/ERVeepp/docs/` 知识库 + 项目代码仓库整理，并经本人人工核对（内容属实）
- **量化评分来源**：RAG 命中率（代码 80% / 多模态 100% / 资产 100%）走自建**黄金集评测**（Ollama `nomic-embed-text` embedding + Qdrant 检索 + 回归脚本）；AI 能力走**评测四层**（语法 / 契约 / 黄金集前置门禁 + **LLM-as-Judge** 四维打分抽检）
- **可复现**：数据与脚本本地可复现
