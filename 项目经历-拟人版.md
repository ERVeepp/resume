# 项目经历

> 用途：直接上简历。第一人称叙述，技术栈融入描述，保留可信量化。
> 生成：2026-08-25（拟人化版本；AI 生成的旧版保留在「项目经历-AI原版.md」）

---

## 广告平台前端系统（web_youngs）

**时间**：2021.10 - 2026.05 ｜ **角色**：核心前端开发

**项目描述**：在这套程序化广告平台做了四年多，从 DSP 投放管理、报表分析、受众定向一路做到 SSP 媒体侧和 ADX 对接，前后端一起写，独立交付了 40+ 页面，系统支撑日均百万级广告请求。技术上最值得一提的有三件：一是把 CSV/Excel 解析用 Rust 编译成 WASM 重写，原来 JS 处理 30 秒起步、大文件直接卡死，重写后 2 秒内出结果，快了一个数量级；二是给平台接了一个自建的自然语言查数 Agent，运营人员直接问问题就能出报表，不用再依赖写 SQL 的开发；三是做了一个 Chrome 扩展，把 AppsFlyer 多账号的切换和导出自动化，运营每天省下半小时。

**项目业绩**：
- 独立交付 40+ 页面，覆盖 DSP/SSP 全链路
- Rust/WASM 重写 CSV/Excel 解析，性能提升 15 倍（30s→2s）
- 接入自然语言查数 AI Agent，运营告别手写 SQL
- Chrome 扩展自动化多账号管理，日均省运营 30 分钟

---

## 落地页低代码平台（Landing Page Builder）

**时间**：2026.06 - 2026.08 ｜ **角色**：核心开发者

**项目描述**：一个让广告主自己拖拽就能做落地页的低代码平台，我负责了从编辑器到运行时 SDK 到 Mock 服务的整套东西（Vue 3 + NestJS + Redis + pnpm Monorepo）。编辑器做可视化搭建，SDK 编译成 IIFE 内联到第三方页面去渲染落地页、采行为数据，两边靠一套组件 JSON-Schema 协议对齐，避免"编辑器写的页面 SDK 渲染不出来"这类对不上。反作弊做了 6 层（Bot 检测、一次性令牌、请求签名、限流、白名单），扛住了 Puppeteer/Playwright 的脚本刷量。后面加了个 AI 编排器，把页面交互（条件、触发、埋点、分支）做成 Vue Flow 节点图，AI 直接拿自然语言生成节点图，运行时由解释器驱动页面——关键是没动 EditorComponent 的 schema，老页面零改动兼容。

**项目业绩**：
- 编辑器 + SDK + Mock 服务全栈，支撑广告投放业务
- 6 层反作弊扛住 Puppeteer/Playwright 脚本刷量
- AI 编排器：节点图 + DAG 校验 + 运行时解释器 + AI 生成，32 单测，向后兼容
- 体积门禁 CLI 管控 SDK 产物大小

---

## 广告协议引擎（Ad Protocol Engine）

**时间**：2016.11 - 至今 ｜ **角色**：独立开发

**项目描述**：广告协议这块没有直接调现成 SDK，而是从零造了一个 IAB 协议引擎（TypeScript + Rust/WASM）。核心思路是三层：TypeScript 管生命周期和端适配，Rust 编译成 WASM 做解析（VAST XML、MRAID 状态机），再加一层 Map 驱动的分发做协议版本路由。产物打包成 CJS/ESM/IIFE/DTS 四种格式，Node、浏览器、第三方内联都能用。Bridge 抽象了 iOS/Android/Web/CTV/OTT 五端，一套接口屏蔽平台差异，MRAID 3.0 和 VAST 4.0 的完整生命周期都覆盖。

**项目业绩**：
- 98 个单测 + 27 个合规测试，覆盖 MRAID 3.0 / VAST 4.0 完整协议生命周期
- Rust/WASM 解析单次 7.6µs，Rust 所有权/RAII 保证内存安全（无 GC 停顿）
- 四格式产物（CJS/ESM/IIFE/DTS）覆盖 Node/浏览器/第三方页面内联全场景
- 五端 Bridge（iOS/Android/Web/CTV/OTT）统一接口抽象，一套代码屏蔽平台差异
- Map 驱动协议分发：协议版本路由 + 扩展点注入，新协议版本零侵入接入

---

## 自动化反作弊验证工具（Ad Automation）

**时间**：2024.05 - 至今 ｜ **角色**：独立开发

**项目描述**：为了验证自己的反作弊体系到底扛不扛打，我做了一套红蓝对抗工具（TypeScript + Playwright + Chrome CDP + ADB）。红队这边用 Playwright 连真机 Chrome（ADB forward）模拟真人操作，行为仿真度做到 85%+——绕过 webdriver 标记、Canvas 指纹伪装、随机延时加贝塞尔曲线，让防护系统分不清真人和脚本。蓝队搭了个归因模拟平台，把 AppsFlyer 和 Google Ads 的双归因、Protect360 6 条规则、Google 7 条规则都模拟出来。红队准备了 5 种攻击方案（curl 打满、Playwright+指纹库、真机 CDP、CloakBrowser），一共规划了 11 种攻击场景。攻下来确实发现不少盲区，比如 WebDriver 检测能被绕过、请求时序异常识别不够，防守侧 6 层防护就是照这些结果迭代加固的。

**项目业绩**：
- 攻防一体：攻击侧找出 WebDriver 绕过、时序识别盲区，防守侧据此加固
- 蓝方双归因模拟 + 红方 5 攻击方案，11 种攻击 spec，拆解 AppsFlyer 四层防御 + 15 检测维度
- 真人行为仿真 85%+，自动化覆盖率 90%+，验证效率提升 3 倍

---

## AI 辅助开发体系

**时间**：2025.11 - 至今 ｜ **角色**：独立搭建

**项目描述**：这套东西解决的是"AI 写代码质量不稳"的问题。我把自己的规范、踩坑记忆、技能、Agent 组织成一套三层资产（instructions/memories/skills+agents），用一个编译期脚本按项目技术栈标签精准注入——`projects.yaml` 定义技术栈，脚本展开成 glob，一次性分发到 6 个编辑器，注入从 8 份收敛到 4 份。需求这块，我把 spec 流程升级成了 openspec 基座 + 门禁插件（歧义扫描、强制澄清、契约验收、黄金集回归），60 多个活跃 spec 从需求到归档全程有机器把关。还做了两件外延的事：一个 VS Code 插件（已上架 Marketplace），一个 Electron 桌面壳（内置 Node 运行时、进程托管、版本管理，用户免装 Node）。

**项目业绩**：
- 23 规范 + 23 记忆 + 20 技能 + 6 Agent（70 资产），注入精准度提升 40%
- Spec 驱动（openspec + 门禁插件）60+ 活跃 spec 全链路闭环
- 资产本地 RAG（Qdrant copilot_docs，黄金集命中 100%）；docs 知识库 6 类重构
- VS Code 插件已上架；DSH 桌面壳内置 Node 运行时 + 进程托管

---

## DSH 插件生态开发（dsh-plugins）

**时间**：2026.08 - 至今 ｜ **角色**：独立开发

**项目描述**：给 DeepSeek Harness（一个"一切皆插件"的 Agent Harness）写了两个插件并真正上线，从 GitHub topic 到 1024Store 插件市场到 npm 发布，整条链路走通。一个叫 npm-advisor，帮 Agent 在引入依赖前判断靠不靠谱——拉 npm registry 的元数据看 deprecated、维护活跃度，内置了一张"原生替代/弃坑包"映射表，让模型自己裁决选型。另一个叫 hardware-benchmark，读本机硬件打分（CPU/内存/磁盘/GPU/网卡），分工程开发和游戏两个维度，还能实测网速，给出 DIY 玩家口吻的升级建议。设计上坚持采集和评分分离，用 mock 数据就能跑测试，不依赖真机。

**项目业绩**：
- 两插件全流程上线：GitHub topic → 1024Store 收录 → npm 发布
- npm-advisor：原生替代映射 + README 迁移声明识别，帮模型判断弃坑包/换代
- hardware-benchmark：双维评分 + 网速实测 + 升级建议，13 项 mock 断言
- 沉淀发布 SOP（2FA/传播延迟等坑）

---

## AppsFlyer 数据抓取工具（appsflyer-bot）

**时间**：2026.04 - 2026.05 ｜ **角色**：独立开发

**项目描述**：运营经常要在 AppsFlyer 后台做 agency allowlist 握手这类操作，得逐账号手动登录、输验证码，很繁琐。我用 NestJS + Playwright + BullMQ 写了个任务自动化服务：从 Youngs 平台或飞书发一条任务，服务端每 30 秒轮询拉取，进 Redis 队列，然后 Playwright 自动登录（2FA 验证码也自动生成）、执行操作、回写执行结果，全程无人值守。飞书那边接了个机器人，用 LLM 把运营的自然语言指令解析成结构化任务——运营直接说"给 xx 账号加 agency"就行。浏览器操作抽象成可扩展的 action 架构，新增一类操作只需写一个 action 类注册进去。安全上做了 IP 白名单 + API Key 双重鉴权。

**项目业绩**：
- 端到端无人值守：任务创建 → 轮询 → 队列 → 自动化执行 → 结果回写全链路打通，替代逐账号手动操作
- 2FA TOTP 自动生成，登录环节零人工
- 飞书机器人 + LLM 自然语言解析，运营一句话下发任务，零门槛
- 可扩展 action 架构（BaseAction + 注册表），新增操作零侵入
- BullMQ 队列：优先级 + 失败重试 + 并发控制，任务可靠执行不丢单
- IP 白名单 + API Key 双重鉴权

---

## AI Agent 平台（matrix-ai-agent · 升级为长任务 Agent 平台）

**时间**：2026.04 - 至今 ｜ **角色**：独立开发

**项目描述**：从零搭的多 Agent 平台（NestJS + OpenAI + Qdrant + LangGraph），并升级为长任务执行平台。底座是"每个 Agent 独立单元"的设计——自己的系统提示词、工具链、上下文窗口，靠路由层（关键词 + LLM 意图分类）分发；agent-core 把 Agent 基类、工具注册、会话管理、LLM 适配器抽成独立库与业务解耦、单独打包（已落地 web_youngs）。检索做了三种 RAG（文档 993 chunks 带溯源/多租户、代码 tree-sitter 80%、多模态 CLIP 100%）；LangGraph 可选引擎 checkpoint 落 SQLite 跨进程断点续跑；评测四层（语法/契约/黄金集前置门禁 + LLM-as-Judge 抽检）；批量生成流水线（模板+参数出变体→门禁→自动分支/PR→CI 防退化）；脚本生成 Agent（一句话→沙箱验证→注册工具→业务调用→失败样本回流）。在此基础上**升级为长任务执行平台**：长任务可靠执行（断点续跑 / 重试 / 心跳 / 预算熔断，Temporal 编排设计中）+ **广告全链路 Agent 化**——投放 Agent（一句话起投 / 调价 / 防超预算，data_query 挂 8 个投放工具）、报表 Agent（查 ClickHouse 出周报）、巡检 Agent（素材批量巡检 + 告警）、竞品分析 Agent；花钱动作过**写三档权限（读自主 / 写确认 / 敏感双人）+ 门禁 + 审计**，决策输出可解释 JSON（`expected_impact` 锚点回测决策质量）；对接 SSP 广告平台（go_youngs）：Agent 工具直调其 HTTP API（建 offer / 查报表 / 巡检）。同时调研了记忆机制（Memmy / Mem0 / Graphiti 选型 + 记忆生命周期设计）。

**项目业绩**：
- 三形态 RAG：文档（993 chunks）+ 代码（tree-sitter 80%）+ 多模态（CLIP 100%）
- LangGraph 引擎：SQLite checkpoint 跨进程持久化 + 节点级断点续跑 + subgraph
- 评测四层前置门禁（语法/契约/黄金集）+ LLM-as-Judge 抽检
- batch-generate：批量变体 → 门禁 → 自动分支/PR + CI 回归门禁
- 脚本生成 Agent：一句话 → 沙箱验证 → 注册工具 → 业务调用 → 失败回流（4 Wave 落地）
- 长任务可靠执行：断点续跑 / 重试 / 心跳 / 预算熔断（LangGraph checkpoint 断点续跑已落地；Temporal 编排设计中）
- 投放 Agent：data_query 挂 8 个投放工具，写操作过门禁 + 审计（已落地）
- 决策契约：可解释决策 JSON + expected_impact 评测锚点，决策质量可回测（已落地）
- 广告全链路场景：**投放 Agent 已落地**（一句话起投 / 调价 / 防超预算，5 Go 服务 + spec 归档可演示）；报表 / 巡检 / 竞品持续推进；对接 SSP（go_youngs）：Agent 工具直调其 API
- 记忆机制研究：Memmy/Mem0/Graphiti 选型 + 记忆生命周期设计

---

## 开源组件库（dragsection）

**时间**：2023 ｜ **角色**：独立开发

**项目描述**：一个开源的小组件，Vue 3 的拖拽区间选择器，发布在 npm 上。当时项目中要选连续时间段，找不到合适的，就自己写了一个，干脆开源出去。MIT 协议，现在有 9 个下游项目在用。

**项目业绩**：
- npm 发布，获 9 个下游项目依赖
- 独立完成设计/开发/打包/发布全流程

---

## AI 全自动短视频生成引擎（Pixelle-Video）

**时间**：2026.07 - 至今 ｜ **角色**：深度实践 / 集成应用

**项目描述**：研究 AI 全自动短视频怎么跑通。基于开源的 Pixelle-Video，把"给个主题就出成片"的整条流水线搭了起来：LLM 写解说文案 → 文生图/文生视频（接了 Seedream、Seedance、Kling、WAN 2.1 几个模型）→ TTS 配音 → 配乐 → 合成。跑下来对多模态生成模型的脾气有了数——哪个适合什么场景、能力边界在哪，也理解了这类平台"LLM 编排 + 模型调度 + 组装"的架构。

**项目业绩**：
- 端到端跑通「主题 → 完整视频」，零剪辑经验产出成片
- 掌握多模态模型生态（Seedream/WAN/Kling）能力与选型差异

---

## 广告投放 Agent 全链路系统（ad-tech-labs）

**时间**：2026.07 - 至今 ｜ **角色**：独立开发

**项目描述**：先做了一个程序化竞价引擎（Go Bidder 接 OpenRTB 2.5，竞价日志走 Kafka 进 Flink 做 5 秒窗口聚合，落 ClickHouse 出实时指标），踩过一个坑：竞价日志乱序会导致窗口错配，最后用事件时间 + Watermark 解决。接着把它演进成投放自动化全链路，补了 5 个 Go 服务：一句话起投的交互层、带多租户 RBAC 和花钱门禁的执行层、预算熔断和异常告警的风控、读实时指标出调控指令的策略引擎（配黄金集评估防改坏）、DCO 动态组材的创意层、AB 验证的验证层。整套可以现场演示。

**项目业绩**：
- 端到端实时链路（Header Bidding → Go Bidder → Kafka → Flink → ClickHouse），5s 窗口出 RPS/胜率/eCPM
- 投放 Agent 全链路 5 服务：一句话起投 + RBAC + 门禁 + 熔断 + 策略引擎 + DCO + AB lift
- 端到端实测通过：改价落地、预算熔断、viewer 403、AB lift +15%、一句话起投成功
- CI/CD：多语言镜像 + GitHub Actions

---

## 终端最小 Coding Agent（agent-loop-tui）

**时间**：2026.08 ｜ **角色**：独立开发

**项目描述**：想搞清楚 Coding Agent 到底是怎么转起来的，就照着核心链路自己复刻了一个终端版最小实现（Node.js + Ink + TypeScript）：Ink 写的 TUI 界面，加上 Agentic Loop（工具调用循环、上下文管理、最大轮次防死循环）。拆解 OpenCode 源码后，对"超长上下文怎么处理"有了具体答案——摘要压缩 + 重建请求 + 可回溯。

**项目业绩**：
- Ink TUI + Agentic Loop 完整复刻，可交互的终端 Coding Agent
- 拆解 OpenCode 核心链路，沉淀超长上下文处理方案
